app OurDesk {
  "The standard helpdesk from the registry, with our own title."
}

extends support.helpdesk

override text pageTitle = the text users see for @page: "Our inbox", "Reports" or "Settings" as title

drop example "the inbox at a glance"

example "our title" {
  see pageTitle = "Our inbox"
}
